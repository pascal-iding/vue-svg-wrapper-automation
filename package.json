{
  "name": "vue-svg-wrapper-automation",
  "displayName": "Vue svg wrapper automation",
  "description": "Create a vue svg wrapper from your clipboard.",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.103.0"
  },
  "categories": [
    "Snippets",
    "Other"
  ],
  "activationEvents": [
    "workspaceContains:**/*.vue"
  ],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "vueSvgWrapper.svgToVue",
        "title": "Svg to vue"
      }
    ],
    "keybindings": [
      {
        "command": "vueSvgWrapper.svgToVue",
        "key": "ctrl+alt+shift+s",
        "mac": "cmd+alt+shift+s",
        "when": "editorTextFocus"
      }
    ],
    "configuration": {
      "title": "Vue SVG Wrapper Automation",
      "properties": {
        "vueSvgWrapper.defaultDirectory": {
          "type": "string",
          "default": "",
          "description": "The .vue file will be generated inside this directory. The workspace root is used by default (Empty value). You can set a path relative to your workspace root (eg. assets/icons/)."
        },
        "vueSvgWrapper.useCompositionApi": {
          "type": "boolean",
          "default": true,
          "description": "Will generate a composition api or options api component."
        },
        "vueSvgWrapper.customTemplate": {
          "type": "string",
          "description": "Custom Vue template to use. Use {{SVG_CONTENT}} as placeholder for the SVG content and {{COMPONENT_NAME}} for the component name. Leave empty to use default template.",
          "editPresentation": "multilineText"
        },
        "vueSvgWrapper.allowDownloadWithHTTPS": {
          "type": "boolean",
          "default": true,
          "description": "Allow downloading SVGs from HTTPS URLs. When disabled, only clipboard content will be used."
        }
      }
    }
  },
  "scripts": {
    "lint": "eslint extension.js 'commands/**/*.js'",
    "pretest": "npm run lint",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.103.0",
    "@vscode/test-cli": "^0.0.11",
    "@vscode/test-electron": "^2.5.2",
    "eslint": "^9.32.0",
    "eslint-config-google": "^0.14.0"
  }
}
